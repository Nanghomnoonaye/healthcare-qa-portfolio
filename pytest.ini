[pytest]
addopts = -v --tb=short
markers =
    smoke: fast critical-path checks
    auth: authentication tests
    data_integrity: boundary value / input validation tests
    ui: layout and rendering stability tests
